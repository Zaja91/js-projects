// A Promise is an obj that returns some value in the future that you expect to get
// It has 3 states: Pending, Fullfilled, Refected

const user = {
  name: "Andrei",
  surname: "Ilinca",
};

const { name, surname } = user;
console.log(user.name, user.surname);
